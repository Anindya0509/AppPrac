import { AlertNotification } from './alert-notification.model';

describe('AlertNotification', () => {
  it('should create an instance', () => {
    expect(new AlertNotification()).toBeTruthy();
  });
});
